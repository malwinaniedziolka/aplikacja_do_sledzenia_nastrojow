const HOME_LINK = {
	label: 'Home',
	path: '/',
};

const MENU_LINKS = [
	HOME_LINK,
	{ label: 'Add entry', path: '/add' },
	{ label: 'History', path: '/history' },
	{ label: 'Stats', path: '/stats' },
];

module.exports = {
	MENU_LINKS,
};
